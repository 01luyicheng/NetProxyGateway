# Gradle
.gradle/
**/build/
android/b/
android/.gpc/

# Android local config
android/local.properties

# IDE
.idea/
.vscode/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Go binaries
*.exe
server/api/api
server/socks5-proxy/socks5-proxy
server/tunnel/tunnel

# Tooling
.sisyphus/

# Git hooks are tracked in git, but local hooks directory might have other files
# .githooks/* (we track specific hooks, ignore local-only hooks)

# Commitlint (config is tracked, node_modules would be ignored if installed locally)
node_modules/

# Temporary files generated during development/review
diff_output.txt
staged.txt
staged_diff.txt
temp_diff.txt
unstaged.txt
*.tmp
