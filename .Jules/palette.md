## 2024-07-19 - Add confirmation dialog and disabled state for destructive actions
**Learning:** Users can accidentally trigger destructive actions (like clearing logs) if there is no confirmation dialog. Additionally, buttons that perform actions on a list should be disabled when the list is empty to provide clear feedback on the system's state.
**Action:** Always add a confirmation dialog for destructive actions and disable buttons that are not currently actionable.
